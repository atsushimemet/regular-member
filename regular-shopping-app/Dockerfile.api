FROM node:18-alpine

WORKDIR /app

# package.jsonをコピーして依存関係をインストール（開発用なので全てインストール）
COPY package*.json ./
RUN npm ci

# サーバーファイルをコピー
COPY server/ ./server/

# ポートを公開
EXPOSE 3001

# サーバーを起動
CMD ["npm", "run", "server"]
