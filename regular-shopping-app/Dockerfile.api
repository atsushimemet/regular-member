FROM node:18-alpine

WORKDIR /app

# package.jsonをコピーして依存関係をインストール
COPY package*.json ./
RUN npm ci --only=production

# サーバーファイルをコピー
COPY server/ ./server/

# ポートを公開
EXPOSE 3001

# サーバーを起動
CMD ["npm", "run", "server"]